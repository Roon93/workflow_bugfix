# Claude Code 代码库自动修复/开发插件

## 1. 产品概述

### 1.1 产品名称
workflow_bugfix

### 1.2 产品定位
这是一个面向 **既有代码库** 的 Claude Code 插件，用于在本地仓库中自动完成：
- Bug 修复
- 老项目小功能开发

插件以 **Bug 修复优先** 为主场景，强调：
- 自动分析输入
- 自动检索上下文
- 自动生成/运行测试
- 自动修改代码
- 自动循环验证
- 可回退、可审计、可复原

### 1.3 核心价值
1. 降低老代码库定位 bug 的成本
2. 提升小功能开发效率
3. 让测试描述、日志、现象输入能够直接转化成可执行修复任务
4. 在本地闭环完成分析、修改、验证和回退

### 1.4 参考项目
1. https://github.com/Roon93/workflow_dev
2. https://github.com/zilliztech/claude-context
3. https://github.com/Mibayy/token-savior
4. https://github.com/tirth8205/code-review-graph

---

## 2. 背景与问题

### 2.1 现状问题
在老项目或嵌入式 Linux C/C++ 项目中，常见痛点包括：
- 代码复杂、上下文分散
- 测试部常只提供现象和日志
- 根因定位耗时
- 修复后回归范围不明确
- 修改构建文件、测试文件时风险高
- 需要多轮尝试才能稳定修复

### 2.2 机会
Claude Code 已具备代码理解与执行能力，但缺少：
- 面向仓库任务的明确工作流
- bug 修复闭环
- 可回退机制
- 多层上下文检索
- 多 agent 并行协作
- 面向日志输入的分析流程

---

## 3. 目标用户

### 3.1 主要用户
- 嵌入式 Linux C/C++ 开发者
- 测试/开发协作场景中的维护工程师
- 老项目维护人员

### 3.2 次要用户
- TypeScript / Python / JavaScript 项目开发者
- 需要快速修 bug 的应用工程师

### 3.3 使用者特征
- 熟悉 git、构建、测试
- 面对的是已有代码库
- 更关心修复准确性、回归风险和效率，而不是花哨功能

---

## 4. 产品目标

### 4.1 业务目标
- 提升 bug 修复速度
- 降低上下文分析成本
- 减少修复后回归
- 支持老项目小功能的最小改动开发

### 4.2 产品目标
- 在本地仓库内自动完成从输入到修复的闭环
- 对模糊 bug 输入（现象 + log）进行结构化分析
- 支持 TDD + loop 修复策略
- 支持 git checkpoint / rewind
- 支持主 agent + subagent 并行协作
- 支持双输出：报告 + JSON

---

## 5. 范围定义

## 5.1 In Scope
1. Bug 修复优先工作流
2. 老项目小功能开发工作流
3. 输入分析：现象、日志、失败输出、崩溃栈
4. 仓库上下文检索
5. 多层上下文：
   - 文件级
   - 符号级
   - 路径级
   - 图谱级
6. TDD + loop
7. 本地测试执行
8. gtest 与自定义脚本支持
9. git 自动 checkpoint
10. rewind
11. 多 subagent 并行
12. 双输出
13. 本地状态文件 + SQLite 数据存储

## 5.2 Out of Scope
1. 远程设备自动执行
2. 云端编排平台
3. 大型长期知识库系统
4. 复杂可视化编辑器
5. 自动提交到远端仓库
6. 完整 CI/CD 平台替代

---

## 6. 核心使用场景

## 6.1 场景 A：测试部只给现象和日志
### 输入
- “某接口偶发返回失败”
- 日志片段
- 崩溃堆栈
- 部分复现步骤

### 用户期望
- 自动分析问题
- 判断可能模块
- 生成复现策略
- 自动定位根因
- 自动修复并验证

---

## 6.2 场景 B：老项目修一个小需求
### 输入
- “给某模块增加一个兼容参数”
- 现有仓库
- 约束条件

### 用户期望
- 找到相似实现
- 识别改动边界
- 先补测试
- 实现功能
- 验证通过

---

## 6.3 场景 C：失败测试修复
### 输入
- 单测失败输出
- gtest 失败信息
- 自定义脚本失败结果

### 用户期望
- 复现失败
- 定位代码
- 修复
- 回归验证

---

## 7. 产品原则

1. **自动执行优先**
   - 能自动做的尽量自动做

2. **TDD + loop 必须遵守**
   - 先复现/测试，再修复，再验证

3. **最小改动原则**
   - 优先局部修复，避免无关重构

4. **可回退原则**
   - 每次关键动作自动 commit，保留 git 记录

5. **上下文最小充分原则**
   - 只加载完成任务所需内容

6. **主 agent 决策，subagent 执行**
   - 主 agent 负责派发和汇总
   - subagent 负责具体任务

---

## 8. 功能需求

---

### 8.1 任务输入与分类

#### 功能描述
系统接收用户输入后，自动识别任务类型：
- Bug 修复
- Feature 开发
- 仅分析

#### 功能要求
- 支持自然语言输入
- 支持日志片段输入
- 支持失败测试输出输入
- 支持部分信息缺失的 bug 报告

#### 输出
- 任务类型
- 问题摘要
- 所需补充信息
- 初始执行建议

---

### 8.2 Bug 输入分析

#### 功能描述
对“现象 + log”的输入进行结构化分析。

#### 功能要求
- 提取关键日志线索
- 识别模块、错误码、栈信息
- 生成根因假设
- 判断复现可能性
- 输出最小复现策略

#### 输出
- 问题分类
- 关键线索
- 候选根因
- 复现建议
- 阻塞信息

---

### 8.3 仓库上下文检索

#### 功能描述
自动从代码库中找出与任务最相关的上下文。

#### 功能要求

检索分为四层，从点到线到面逐步展开：

1. **文件级**：找到与任务相关的文件，读取关键片段
2. **符号级**：定位具体的函数/类/接口/方法，查找依赖引用
3. **路径级**：追踪调用链、数据流、错误传播链
4. **图谱级**：构建模块依赖图、业务链路图、测试覆盖图（详见 8.4）

- 支持嵌入式 Linux C/C++ 优先
- 次级支持 TS/Python/JS

#### 输出
- 相关文件列表
- 相关函数/类/符号
- 相关调用链
- 相关测试
- 推荐查看顺序

---

### 8.4 图谱分析

#### 功能描述
8.3 图谱级检索的具体能力展开。构建并使用仓库关系图来缩小搜索范围。

#### 功能要求
- 调用关系分析
- 依赖关系分析
- 影响范围分析
- 测试覆盖映射

#### 输出
- 影响面
- 候选根因位置
- 相关验证点

---

### 8.5 TDD 工作流

#### Bug 场景
- 先复现失败
- 再修复
- 再验证

#### Feature 场景
- 先补测试
- 再实现
- 再验证

#### 要求
- 测试优先
- 失败即 loop
- 每次循环必须记录证据

---

### 8.6 自动测试执行

#### 功能描述
系统在本地自动执行测试。

#### 支持范围
- gtest
- 自定义脚本

#### 功能要求
- 支持指定测试目标
- 支持读取 stdout/stderr
- 支持失败摘要提取
- 支持结果归档

---

### 8.7 自动修复与 loop

#### 功能描述
测试失败后，系统自动分析失败原因并继续修复。

#### 功能要求
- 最多重试 5 次，可配置
- 每轮都必须保留记录
- 每轮都必须输出状态
- 支持重构到最近 checkpoint

---

### 8.8 git checkpoint / rewind

#### 功能描述
所有关键阶段自动 commit，便于校验和回退。

#### 功能要求
- 每个关键阶段自动 commit
- 支持查看 diff
- 支持 rewind 到最近成功状态
- 支持回退文件变更和状态

#### 关键阶段
- 输入分析完成
- 复现测试完成
- 修复实现完成
- 验证完成
- 每轮 loop 结束

---

### 8.9 多 subagent 并行

#### 功能描述
主 agent 派发任务，subagent 并行执行，最后汇总。

#### 功能要求
- 主 agent 负责派发和决策
- subagent 负责执行单一任务
- subagent 完成后必须回报状态
- 支持并行进行分析、检索、测试、修复相关任务

---

### 8.10 状态与数据存储

#### 功能描述
保存任务状态与较大规模的分析数据。

#### 功能要求
- 本地文件：保存轻量状态
- SQLite：保存大数据量索引/图谱/历史信息

#### 本地状态示例
- 当前阶段
- loop 次数
- 当前 checkpoint
- 当前假设

#### SQLite 数据示例
- 索引结果
- 图谱数据
- 历史任务
- 测试结果摘要
- 失败样本

---

### 8.11 输出格式

#### 功能描述
任务完成后输出两种结果：
1. 人类可读报告
2. 结构化 JSON

#### 功能要求
- JSON 必须有 `schema_version`
- 报告要包含关键结论、改动、测试和风险
- JSON 要可供上层系统消费

---

## 9. 非功能需求

### 9.1 性能
- 上下文检索应尽量快
- 并行 subagent 应减少总耗时
- loop 不应无限发散

### 9.2 可维护性
- 模块化设计
- 清晰的任务状态机
- 清晰的 JSON schema
- 可配置的 loop 次数和 checkpoint 策略

### 9.3 可审计性
- 所有关键动作必须有 git 记录
- 可查看 diff
- 可追踪每轮 loop
- 可复盘任务执行过程

### 9.4 可靠性
- 修复失败后可 rewind
- 状态与代码变更分离
- 任务中断后可恢复

---

## 10. 成功标准

### 10.1 A：找上下文更准
- 更快找到相关文件/符号/路径
- 减少无关上下文

### 10.2 C：修改更少但更准
- 最小 diff
- 少引入回归
- 变更集中

### 10.3 E：bug 定位更快
- 更快形成可验证假设
- 更快定位根因
- 更快完成修复闭环

---

## 11. 关键约束与边界

### 11.1 自动修改边界
允许自动修改：
- 代码文件
- 测试文件
- 构建文件

但必须：
- 有 git 记录
- 可校验
- 可 rewind

### 11.2 执行环境
默认仅支持：
- 本地执行

### 11.3 重试次数
- 默认 5 次
- 可配置

### 11.4 输出要求
- 双输出
- JSON 结构稳定
- 保留报告可读性

---

## 12. MVP 范围

### 必做
1. bug 输入分析
2. 上下文检索
3. gtest / 脚本复现
4. 自动修复
5. 本地验证
6. loop
7. git checkpoint / rewind
8. 主 agent + subagent
9. 双输出
10. 本地状态文件 + SQLite

### 暂缓
1. 远程设备支持
2. 多仓库知识网络
3. 高级可视化
4. 复杂人工确认流程
5. 云端编排

---

## 13. 风险与挑战

### 13.1 日志信息不足
测试部输入可能不完整，需依赖假设驱动分析。

### 13.2 老代码库复杂
嵌入式 C/C++ 项目可能缺少规范测试，复现和定位难度高。

### 13.3 自动修改风险
自动改构建文件和测试文件有较大风险，因此必须依赖 git checkpoint。

### 13.4 loop 发散
如果根因判断错误，可能反复修改，因此需要明确重试上限和 rewind 机制。

---

## 14. 验收标准

产品可认为达到 MVP 的标准是：

1. 能处理“现象 + log”的 bug 输入
2. 能自动找出相关上下文
3. 能自动生成/使用复现测试
4. 能在本地自动修复并验证
5. 能在失败时 loop，最多 5 次
6. 能自动 commit 并 rewind
7. 能输出报告 + JSON
8. 能支持主 agent 派发、subagent 执行的协作模式